# Page-specific design overrides

Drop a `<page-name>.md` here only when a specific surface must deviate from
`../MASTER.md`. When building that page with **frontend-design**, the page file's rules
**override** the Master for that page; otherwise the Master applies in full.

Keep overrides minimal and justified — the Master is the default for everything.
